const sessionRouter = require("./sessionRouters");
const userRouter = require("./userRouters");
const bookRouter = require("./bookRouters");

module.exports = {
    sessionRouter,
    userRouter,
    bookRouter,
}